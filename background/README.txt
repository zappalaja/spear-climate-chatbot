Place exactly one image file here (.jpg, .png, .webp, etc.) to use as the chatbot's background image.
The first image found (alphabetically) will be used automatically.
Recommended: high-resolution landscape image (e.g. 1920x1080).
If this folder is empty, no background image is applied.
